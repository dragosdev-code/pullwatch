---
import { themeInitInlineScript } from "../lib/theme-init-snippet";
---

<!-- Apply saved DaisyUI theme before paint (same key as the extension). -->
<script is:inline set:html={themeInitInlineScript} />
