/** First paint before the vanilla store mounts inside `SquashMinigame`. */
export function SquashShellBooting() {
  return (
    <div
      data-testid="squash-shell-loading"
      className="flex min-h-0 flex-1 items-center justify-center p-4 text-xs uppercase"
    >
      booting
    </div>
  );
}
