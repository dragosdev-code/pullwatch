import { RefreshButtonShell } from './refresh-button-shell';
import type { RefreshButtonProps } from './types';

export const RefreshButton = (props: RefreshButtonProps) => <RefreshButtonShell {...props} />;
